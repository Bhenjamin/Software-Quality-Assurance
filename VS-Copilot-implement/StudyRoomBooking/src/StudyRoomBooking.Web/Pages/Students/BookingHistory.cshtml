@page
@model BookingHistoryModel
@{
	ViewData["Title"] = "Booking History";
}

<div class="container mt-4">
	<h2>Booking History</h2>

	@if (Model.BookingHistory.Any())
	{
		<div class="table-responsive">
			<table class="table table-striped">
				<thead>
					<tr>
						<th>Room Name</th>
						<th>Date</th>
						<th>Time</th>
						<th>Status</th>
						<th>Confirmation #</th>
						<th>Created</th>
					</tr>
				</thead>
				<tbody>
					@foreach (var booking in Model.BookingHistory)
					{
						<tr>
							<td>@booking.RoomName</td>
							<td>@booking.BookingDate.ToString("dd/MM/yyyy")</td>
							<td>@booking.StartTime.ToString(@"hh\:mm") - @booking.EndTime.ToString(@"hh\:mm")</td>
							<td>
								@if (booking.Status == StudyRoomBooking.Domain.Enums.BookingStatus.Confirmed)
								{
									<span class="badge bg-success">Confirmed</span>
								}
								else if (booking.Status == StudyRoomBooking.Domain.Enums.BookingStatus.Cancelled)
								{
									<span class="badge bg-danger">Cancelled</span>
								}
								else if (booking.Status == StudyRoomBooking.Domain.Enums.BookingStatus.Completed)
								{
									<span class="badge bg-secondary">Completed</span>
								}
								else
								{
									<span class="badge bg-warning">@booking.Status</span>
								}
							</td>
							<td>@booking.ConfirmationNumber</td>
							<td>@booking.CreatedAt.ToString("dd/MM/yyyy HH:mm")</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	}
	else
	{
		<div class="alert alert-info">No booking history found.</div>
	}

	<div class="mt-3">
		<a asp-page="MyBookings" class="btn btn-primary">My Current Bookings</a>
		<a asp-page="Search" class="btn btn-secondary">Search for Rooms</a>
	</div>
</div>
